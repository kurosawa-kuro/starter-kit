**評価結果**
現状は、前回指摘した Public 公開前ブロッカーを概ね解消済みです。総合評価は **A- / 88点** くらいまで改善しています。Rust/Python/Terraform/GCP 補助、docs、AGENTS の軸に加えて、gitlink 解消、テスト安定化、正典スターターの README / Makefile / `.gitignore` 補完、公開前チェック手順まで整いました。

残る判断事項は、optional 配下の ignored `.env` 系を個別に削除・退避するか、ローカル秘匿ファイルとして残すかです。秘密情報の可能性があるため、自動削除はしていません。

**重大**
- [x] `starters/frontend/admin-pico` が gitlink になっていた問題は解消済み。

  `git ls-files -s starters/frontend/admin-pico` で `160000` は返らず、通常ファイルとして取り込み済み。入れ子の `.git` も削除済み。

- [x] `starters/rust/batch` の `cargo test` 失敗は修正済み。

  [job_registry.rs](/Users/kurosawa/Dev/starter-kit/starters/rust/batch/src/registry/job_registry.rs:18) の global registry に poison 復旧 helper を追加し、[registry_tests.rs](/Users/kurosawa/Dev/starter-kit/starters/rust/batch/tests/registry_tests.rs:8) はテスト間で mutex lock を取る構成に変更済み。`cargo test` 通過済み。

**中**
- [x] 正典スターターの README / Makefile / `.gitignore` 不足は補完済み。

  `starters/python/api-fastapi` と `starters/python/gcp` に README / Makefile / `.gitignore` を追加済み。`starters/rust/api-axum` に README を追加済み。

- [x] [AGENTS.md](/Users/kurosawa/Dev/starter-kit/AGENTS.md:100) の project-generator テンプレート説明は実装と同期済み。

  `doc/04_運用.md` と `doc/README.md` を追記し、[scaffold.rs](/Users/kurosawa/Dev/starter-kit/tools/project-generator/src/scaffold.rs:37) と整合済み。

- [x] 公開前・配布前の掃除手順を追加済み。

  [script/cleanup-generated.sh](/Users/kurosawa/Dev/starter-kit/script/cleanup-generated.sh) と [docs/PUBLIC-RELEASE-CHECK.md](/Users/kurosawa/Dev/starter-kit/docs/PUBLIC-RELEASE-CHECK.md) を追加済み。実行により作業コピーは約 1.1GB から約 135MB まで縮小済み。

**良い点**
- ルート方針、`AGENTS.md`, `CLAUDE.md`, `docs/STARTER-POLICY.md` の方向性はかなり明確です。
- `python-ml` は単体 train から ML pipeline へ整理され、見せ方が良くなっています。
- `script/gcp/` は Public 向けに実値を持たない補助 CLI として良い追加です。
- 簡易 secret grep では、実鍵・実 webhook・典型的 API key らしきものは見当たりませんでした。出ているのは主に `user:password` 形式のサンプルです。

**検証結果**
通過:
- `starters/rust/cli`: `cargo test`
- `starters/rust/api-axum`: `cargo test`
- `starters/rust/batch`: `cargo test`
- `tools/project-generator`: `cargo test`
- `starters/python/batch`: `python3 -m unittest discover -s tests`
- `starters/python/ml`: `make test`
- `starters/python/ml`: `python3 -m unittest discover -s tests`
- `starters/python/gcp`: `make test`
- `starters/infra/terraform`: `terraform fmt -check -recursive && terraform validate`
- `git diff --check` 系
- `script/cleanup-generated.sh`: `bash -n`
- `script/gcp/*.sh`: `bash -n`
- `admin-pico`: gitlink なし、入れ子 `.git` なし

失敗/注意:
- `optional/typescript-workflow-runner`: `vitest: command not found`。依存未 install 状態なので、`npm install` 前提なら説明で吸収可能
- ignored の `.env` 系が optional 配下に残っています。秘密情報の可能性があるため未削除です。
- secret scan は、ドキュメント内の変数名・プレースホルダには反応しますが、実鍵・実 webhook・典型的 API key らしきものは見当たりません。

**優先対応**
1. [x] `admin-pico` の gitlink 問題を解消する。
2. [x] `rust-batch` の registry test を直す。
3. [x] README / Makefile / `.gitignore` が無い正典スターターを埋める。
4. [x] `AGENTS.md` の project-generator 構成を実装と同期する。
5. [x] 公開前クリーンアップ手順と secret scan 手順を追加する。

ここまで直ったため、Public 公開用 starter-kit としてかなり見せられる状態です。最終確認として、コミット前に [docs/PUBLIC-RELEASE-CHECK.md](/Users/kurosawa/Dev/starter-kit/docs/PUBLIC-RELEASE-CHECK.md) の手順で ignored `.env` 系とコミット履歴の確認を行うのがよいです。
